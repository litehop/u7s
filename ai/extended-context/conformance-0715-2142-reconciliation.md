# Conformance run 0715-2142 — full reconciliation (2026-07-16)

**Run:** `temp/e2e/0715-2142-conformance` — full non-disruptive suite, **434 passed / 17 failed / 451 ran** (7135 skipped), completed in ~1h52m (no timeout). First clean full-suite baseline in a while. junit source: `plugins/e2e/results/global/junit_01.xml`; human-readable ginkgo detail is at `podlogs/sonobuoy/sonobuoy-e2e-job-*/logs/e2e.txt` (NOT `plugins/.../e2e.txt`, which is `e2e.log` and omits the test body).

Three read-only scouts (each live-reproduced on its own lima VM, kubectl-first) root-caused all 17. **The 17 collapse to ~9 distinct root causes; 2 are single-node-topology artifacts (not u7s bugs), the rest are narrowly-scoped correctness fixes — mostly "correct core logic, racy/incomplete imperative glue" or "existing in-repo pattern never wired into a second code path." No large missing-feature discovered.**

## The 17 failures → root cause → bead

### Scheduling (6 failures → 3 causes) — Scout A, verified live on lima-node-2
- **delete_pod double-DELETE 409 (4 failures: all Preemption tests).** `delete_pod` (scheduler `lib.rs:1703`) issues two unconditional DELETEs (soft then force, per commit b224bf9f); the 2nd races the 1st's RV bump → `409 Conflict` → `check_delete_response` (`lib.rs:1511`, only tolerates 2xx/404) rejects → `main.rs:224` `?` aborts the whole scheduling cycle → preemptor stuck Pending; only recovers on a passive watch reconnect ~3.5min later, far past test timeout. **Fires even single-preemptor (zero concurrency)** — reproduced live twice. → **mayor-lb60u** (rescope from "concurrent preemption" to "preemption eviction 409, fires solo", elevate P3→P2).
  - **rsei reconciliation:** #816/`4892cc40` (extended-resource accounting) merged 08:54Z, IS in the run binary (ancestor of run HEAD fce7af40). It fixed a DIFFERENT layer (before it, `find_preemption_plan` never ran at all). The 409 is the downstream eviction now failing. rsei stays CLOSED (correct for its scope) — NOT a contradiction, sequential layers. Its close-reason "all 4 pass" is stale (was true via `--focus` at merge; full-suite exposed the 409). Note added to lb60u.
- **pick_node uncached per-node GET undercounts a just-committed CPU request under load** (SchedulerPredicates "resource limits") → incorrect bind onto a full node (kubelet then OutOfcpu-rejects). Correct in isolation; only fails under full-suite concurrency. → **mayor-7pft0** (was P3 perf-only; now a live correctness failure — elevate).
- **emit_scheduling_event lost under load, no timeout/no error** (SchedulerPredicates "NodeSelector not matching"). Scheduler decision correct; the event POST (`lib.rs:1630`, no timeout) never reached the apiserver (a plain re-list 2min later still shows 0 events). → **NEW bead P2**.

### api-machinery CRD/webhook (6 failures → 3 causes) — Scout B, verified live on lima-node-3
- **Conversion-webhook caBundle/TLS bypass (3 failures: both ConversionWebhook + CRFieldSelectors).** `call_conversion_webhook` (`cr.rs:64`) uses the shared global `webhook_client` (`state.rs:375`, `tls_certs_only([cluster_ca])`) — never reads the per-webhook `client_config.caBundle`, so TLS handshake fails against ANY real conversion backend (which ships its own self-signed CA). The correct **per-call** pattern already exists for admission webhooks (`build_webhook_call_client`, `admission.rs:842`, also routes ServiceResolved through konnectivity) — never wired into CR conversion. Makes webhook-strategy CRD conversion 100% non-functional. → **NEW bead P1** (folds in all 3).
- **CR field-validation SSA wording (2 failures) — Rule-7 conflict.** `apply_cr_field_validation` (`cr.rs:786`) always emits `strict decoding error: unknown field "<path>"`. mayor-w1p59/PR #773 (2026-07-14) DELIBERATELY changed it to that wording to fix `CustomResourcePublishOpenAPI` (a plain-CREATE test) — correct for CREATE. But these two FieldValidation tests use `ApplyPatchType` (SSA), which upstream formats with the dotted `.foo: field not declared in schema` wording. Detection is correct; only the message-wording branch on request-type is missing. **Do NOT revert w1p59** (re-breaks PublishOpenAPI) — branch on SSA-Apply vs Create/Update. → **NEW bead P2**.
- **AdmissionWebhook CONNECT/attach deny against a REACHABLE backend (1 failure) — unresolved, needs real-backend follow-up.** Scout B proved matching/invocation/NamespaceSelector/Fail-policy all sound live (fail-closed path denies correctly). But the RUN failure was: attach against a reachable-should-deny backend fully upgraded + hung (did NOT deny). mayor-u6eb/PR #705 fixed the *old* symptom (GET→correct-deny, client silently retries POST/SPDY, old POST route 405'd before admission re-ran; fix added `pod_attach_post`). Current failure looks like the **completeness of that fix on the SPDY-retry path** — the reachable-backend deny isn't enforced/parsed on the POST path. → **NEW bead P2** (repro recipe: deploy real agnhost webhook pod + two-webhook VWC; frame as u6eb-fix-incomplete-on-SPDY-retry, not fully unknown).

### GC / namespace / apps / network (5 failures → 3 real + 2 infra) — Scout C, verified live on lima-node-4
- **GC multi-owner dependent deletion (=mayor-ftkl PANIC-4).** `delete_pods_owned_by` (`resource.rs ~2808`) hard-deletes any pod owned by the deleted RC, ignoring a live *second* owner. Reproduced live: all 4 pods deleted incl. the 2 with a second live owner. The correct `strip_owner_from_resources` pattern exists (used by orphan path) but isn't called here; same in `delete_replicasets_owned_by`/`delete_jobs_owned_by`. P1-grade signature (backs RC/DS/STS/RS/Job foreground cascades). **Distinct from mayor-74j3.5** (that's the `effective_orphan` nil-policy branch racing KCM — zero code overlap). → **NEW bead P2** + rescope **mayor-ftkl** to PANIC-3/5 only.
- **OrderedNamespaceDeletion.** `delete_namespace` (`namespaces.rs:988`) synchronously self-clears the `kubernetes` finalizer inside the DELETE handler → the real KCM namespace controller's trigger (deletionTimestamp set AND "kubernetes" in finalizers) never fires → it never PATCHes `status.conditions` with `NamespaceDeletionContentFailure` → test polls 302s and times out. → **NEW bead (child of mayor-74j3 epic) P2**. Two fix directions (both flagged): (a) let real KCM own it end-to-end, or (b) keep self-cascade but emit the condition u7s is already simulating. Regression risk: `cascade_delete_respects_object_finalizers` (namespaces.rs:3918) locks current fast-path.
- **Proxy scheme:name:port addressing.** `resolve_pod_proxy_target` (`proxy.rs:1379`) does zero URL parsing (literal lookup, port from pod spec) — any `name:port`/`scheme:name:port` pod-proxy 404s. `split_service_port` (`proxy.rs:1756`) `rsplit_once(':')` handles 2-part but the scheme prefix leaks into service name for 3-part. → **NEW bead P2**. (Prior proxy fixes mayor-fgt2/oj7t only did the 2-part suffix for services/nodes; pods never got it, 3-part never handled.)
- **DaemonSet "rollback without unnecessary restarts" + Architecture "two untainted nodes"** — both 2-node precondition failures on single-node lima (`daemon_set.go:451`, `conformance.go:45`), fail in 0.04s before any cluster interaction. Both are already mayor-wz5s7's documented examples (1)&(2), reconfirmed unchanged. Daemonset one is **distinct from mayor-3z4pr** (that's a different DS test, watch-latency). → **UPDATE mayor-wz5s7** (reconfirm timestamp; infra, deferred/WONTFIX-ENV — needs a 2nd VM node).

## Priority read (for the operator reassessment)
Highest-leverage / clearest fixes first:
1. **GC multi-owner (P2, was ftkl-P1)** — small, uses existing pattern, systemic correctness (silent live-pod destruction across all cascades). Arguably still P1 by signature.
2. **Conversion-webhook caBundle (P1)** — makes a whole feature (webhook CRD conversion) functional; copy an existing in-repo pattern; closes 3 failures.
3. **delete_pod 409 (P2)** — closes 4 scheduling failures; small (tolerate 409 like 404 + retry).
These three fixes alone would close **10 of the 17** failures. Then: CR field-validation wording (2), proxy (1), scheduler-event-timeout (1), ns-ordered-deletion (1). Remaining: 2 infra (wz5s7), 1 needs-follow-up (attach-deny reachable backend).

All three fixes #1-3 reuse or mirror existing correct code — low regression risk, high signal. Recommend dispatching them as the first fix wave once the operator confirms priority order.
